#include<stdio.h>

void main() {
    int a,b,i;
    printf("Enter the two intervals:");
    scanf("%d%d",&a,&b);
    for(i=a;i=b;i++)
    {
        if((i%2)==0)
        {
            printf("even");
        }
else
{
    printf(odd number %d",i);

}
        
    }
}
